#include <stdio.h>

struct node {
    struct node* children;
    struct node* parent;
    char data;
} *radixTree;


createRadixTree()
{

}

int main()
{
   // printf() displays the string inside quotation
   printf("Hello, World!");
   return 0;
}